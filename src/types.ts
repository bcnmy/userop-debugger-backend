export type UserOperation = {
    // Define properties and methods relevant to a user operation
}

export type TransactionResult = {
    // Define properties like success, data, error, etc.
}

export type ParsedError = {
    // Define structured error properties
}